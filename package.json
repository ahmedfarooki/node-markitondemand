{
  "name": "node-markitondemand",
  "version": "1.0.0",
  "description": "Node library for dev.markitondemand.com REST APIs.",
  "main": "./lib/markitondemand.js",
  "scripts": {
    "test": "npm run cov && npm run mocha",
    "cov": "./node_modules/.bin/jscoverage lib/ lib-cov",
    "coverage": "make coverage",
    "mocha": "./node_modules/.bin/mocha test -R spec",
    "cov-html": "npm run cov && ./node_modules/.bin/mocha test -R html-cov > coverage.html",
    "coveralls": "npm test && ./node_modules/.bin/mocha test -R mocha-lcov-reporter | ./node_modules/.bin/coveralls",
    "tb": "NODE_COVERAGE=1 ./node_modules/.bin/mocha test -R mocha-lcov-reporter | ./node_modules/coveralls/bin/coveralls.js"
  },
  "author": "Ahmed Farooki",
  "license": "MIT",
  "devDependencies": {
    "mocha": "~2.1.0",
    "nock": "~0.59.1",
    "nock-vcr-recorder-mocha": "~0.3.2",
    "coveralls": "~2.11.2",
    "mocha-lcov-reporter": "0.0.1",
    "jscoverage": "~0.5.9"
  },
  "dependencies": {
    "async": "~0.9.0"
  }
}
